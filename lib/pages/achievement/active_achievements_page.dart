import 'package:flutter/material.dart';

class ActiveAchievementsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Active Achievements Page'),
      ),
    );
  }
}
