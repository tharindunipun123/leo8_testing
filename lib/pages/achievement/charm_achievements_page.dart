import 'package:flutter/material.dart';

class CharmAchievementsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Charm Achievements Page'),
      ),
    );
  }
}
