import 'package:flutter/material.dart';

class RechargeAchievementsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Recharge Achievements Page'),
      ),
    );
  }
}
