import 'package:flutter/material.dart';

class SvipScreen extends StatelessWidget {
  const SvipScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SVIP'),
      ),
      body: Center(
        child: Text('SVIP Screen'),
      ),
    );
  }
}
